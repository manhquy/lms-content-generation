import { LmsGenerationWizard } from '@/features/lms-generation/components/lms-generation-wizard';

export const metadata = {
  title: 'LMS Content Generation Wizard'
};

export default function WizardPage() {
  return <LmsGenerationWizard />;
}
