# Getting Started

```bash
git clone https://github.com/manhquy/lms-content-generation.git
```

```bash
bun install
```

Create a `.env.local` file:

```bash
cp env.example.txt .env.local
```

Add the required environment variables to `.env.local`.

Run the development server:

```bash
bun run dev
```
